## Traefik Dynamic Configuration for Roamie VPN
## Deploy this to: /var/lib/coolify/traefik/dynamic/roamie-vpn.yml on the server

http:
  routers:
    roamie-vpn:
      rule: "Host(`felipenovaesrocha.xyz`) && (PathPrefix(`/api/`) || Path(`/health`))"
      service: roamie-vpn-service
      entryPoints:
        - https
      tls:
        certResolver: letsencrypt

  services:
    roamie-vpn-service:
      loadBalancer:
        servers:
          - url: "http://localhost:8081"
