# Roamie VPN Server - Coolify Environment Variables
# Copy this content to Coolify's environment variables UI
# DO NOT commit actual values to git!

# ============================================
# Database (Internal PostgreSQL)
# ============================================
POSTGRES_USER=roamie
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
POSTGRES_DB=roamie_vpn

# ============================================
# Authentication - REQUIRED
# ============================================
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_GENERATE_RANDOM_SECRET

# Token expiration (default: 7 days)
JWT_EXPIRATION=168h

# Auth code expiration (default: 5 minutes)
AUTH_CODE_EXPIRATION=5m

# ============================================
# Email Service (Resend) - REQUIRED
# ============================================
# Get your API key from https://resend.com
RESEND_API_KEY=re_XXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Sender email (must be verified in Resend)
EMAIL_FROM=noreply@yourdomain.com

# ============================================
# WireGuard Configuration - REQUIRED
# ============================================
# Your server's public IP and WireGuard port
# Format: IP:PORT or DOMAIN:PORT
WG_SERVER_PUBLIC_ENDPOINT=YOUR_SERVER_IP:51820

# WireGuard interface name
WG_INTERFACE=wg0

# WireGuard listen port
WG_PORT=51820

# Network allocation (default values work for most setups)
WG_BASE_NETWORK=10.100.0.0/16
WG_SUBNET_SIZE=29

# ============================================
# Server Configuration
# ============================================
SERVER_PORT=8080

# ============================================
# Device Management
# ============================================
MAX_DEVICES_PER_USER=5
AUTO_REGISTER_DEVICES=true

# ============================================
# SSH Tunnel Configuration
# ============================================
SSH_TUNNEL_PORT=2222
TUNNEL_PORT_RANGE_START=10000
TUNNEL_PORT_RANGE_END=20000

# ============================================
# Firebase (Optional - Mobile App Auth)
# ============================================
# If using Firebase authentication for mobile app:
# 1. Download service account JSON from Firebase Console
# 2. Mount it as a volume or use Coolify's file storage
# 3. Set the path inside the container
# FIREBASE_CREDENTIALS_PATH=/app/firebase-credentials.json
# FIREBASE_CREDENTIALS_FILE=/path/on/host/to/firebase-credentials.json

# ============================================
# Docker Image Configuration
# ============================================
# GitHub repository (for ghcr.io image path)
GITHUB_REPOSITORY=kamikazebr/roamie-desktop

# Image tag: latest, main, sha-xxxxx, or version (v1.0.0)
IMAGE_TAG=latest
