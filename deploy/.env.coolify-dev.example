# Roamie VPN Server - Coolify DEV Environment Variables
# Use this for development environment alongside production
#
# KEY DIFFERENCES FROM PRODUCTION:
# - Different ports: 51821 (WG), 2223 (SSH)
# - Different network: 10.101.0.0/16
# - Different WireGuard interface: wg-dev
# - Different database: roamie_vpn_dev

# ============================================
# Database (DEV)
# ============================================
POSTGRES_USER=roamie_dev
POSTGRES_PASSWORD=CHANGE_ME_DEV_PASSWORD
POSTGRES_DB=roamie_vpn_dev

# ============================================
# Authentication
# ============================================
# Use different secret than production!
JWT_SECRET=CHANGE_ME_DEV_SECRET_DIFFERENT_FROM_PROD

JWT_EXPIRATION=168h
AUTH_CODE_EXPIRATION=5m

# ============================================
# Email Service (Resend)
# ============================================
# Can use same API key as production, or separate for testing
RESEND_API_KEY=re_XXXXXXXXXXXXXXXXXXXXXXXXXXXX
EMAIL_FROM=noreply-dev@yourdomain.com

# ============================================
# WireGuard Configuration (DEV)
# ============================================
# IMPORTANT: Use different port than production (51820)
WG_SERVER_PUBLIC_ENDPOINT=YOUR_SERVER_IP:51821

# Different interface name to avoid conflict
WG_INTERFACE=wg-dev

# DEV port (production uses 51820)
WG_PORT=51821

# Different network range than production (10.100.0.0/16)
WG_BASE_NETWORK=10.101.0.0/16
WG_SUBNET_SIZE=29

# ============================================
# SSH Tunnel Configuration (DEV)
# ============================================
# DEV port (production uses 2222)
SSH_TUNNEL_PORT=2223

# Different port range than production (10000-20000)
TUNNEL_PORT_RANGE_START=20000
TUNNEL_PORT_RANGE_END=30000

# ============================================
# Device Management
# ============================================
MAX_DEVICES_PER_USER=5
AUTO_REGISTER_DEVICES=true

# ============================================
# Docker Image
# ============================================
GITHUB_REPOSITORY=kamikazebr/roamie-desktop
IMAGE_TAG=latest

# ============================================
# Firebase (Optional)
# ============================================
# FIREBASE_CREDENTIALS_PATH=/app/firebase-credentials.json
# FIREBASE_CREDENTIALS_FILE=/path/on/host/to/firebase-credentials.json
